

#include <stdio.h>
#include <stdlib.h>
#include <time.h>

 int RndDig, cnt;
 int ind;

  enum  MyCondition  { Con1, Con2, Con3} Condition;

  int main(){
  
  Condition = static_cast<MyCondition>(Condition+1);

  
  return 0;
}